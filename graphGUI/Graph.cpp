#include "Graph.h"
#include "Renderer.h"

void Graph::Draw(Renderer& renderer)
{
    bool isFirst = true;
    for (int i = -10; i <= 10; i += 0.1) {
        
    }
}

double Graph::F(double x) const
{
    return x * x;
}